{
  "name": "weather-app-monorepo",
  "description": "Angular 21 Weather App with Node.js Proxy",
  "scripts": {
    "build": "cd client && npm install && ng build",
    "start:server": "cd server && npm install && npm start",
    "start:client": "cd client && ng serve --open",
    "start": "npm run build && npm run start:server"
  }
}
