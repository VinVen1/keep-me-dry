import {UnitMeasurePipe} from './unit-measure-pipe';

describe('UnitMeasurePipe', () => {
  it('create an instance', () => {
    const pipe = new UnitMeasurePipe();
    expect(pipe).toBeTruthy();
  });
});
